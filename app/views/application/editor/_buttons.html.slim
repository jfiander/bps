#editor-buttons
  = link_to("Show #{options[:name]}", '#', id: 'show-editor', class: "medium #{auto_show(partial)}")
  = link_to("Hide #{options[:name]}", '#', id: 'hide-editor', class: "medium #{auto_show(partial)}")
#editor class=(auto_show(partial)) = render(partial, options)
