(function() {
  $(function() {
    $('a#show-editor').click(function(event) {
      event.preventDefault();
      $('div#editor').fadeIn(500, "swing");
      $('a.editor').fadeIn(500, "swing");
      $('a#show-editor').toggle();
      $('a#hide-editor').toggle();
      $.ajax({
        type: "POST",
        url: '<%= auto_show_url %>',
        data: {
          'page_name': '<%= page_name %>'
        }
      });
    });
  });

}).call(this);
