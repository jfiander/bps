h1
  | Edit Page
  = FA::Icon.p('markdown', style: :brands, title: 'Markdown')
h2.blue = @page.name.titleize

= render "edit_markdown_form"

small If the above text box is too small, you should be able to drag the bottom right corner to expand it.
hr

= render "markdown_help"
