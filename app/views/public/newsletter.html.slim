h1 Squadron Newsletter
h2 The Bilge Chatter

= editor('newsletter_editor') if current_user&.permitted?(:newsletter, session: session)

h3 Click on a button below to view or download that issue.

- if user_signed_in?
  .information Only the 18 most recent issues (with lighter buttons) are displayed to the public.

= render('bilge_table')

hr
= link_to("https://get.adobe.com/reader/", target: :_blank) { image_tag static_bucket.link("acrobat_reader.png") }
