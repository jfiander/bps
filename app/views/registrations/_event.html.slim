#payment-description
  .subject
    = @event.display_title
    - if @event.course?
      div.type Course
    - elsif @event.seminar?
      div.type Seminar
  .desc on
  .date
    b = @event.start_at.strftime(TimeHelper::PUBLIC_DATE_FORMAT)
    |  at
    b = @event.start_at.strftime(TimeHelper::PUBLIC_TIME_FORMAT)
