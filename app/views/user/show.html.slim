h2 = "#{@profile_title} user"

#profile
  .profile-left Profile Photo
  .profile-right = image_tag @user.photo, width: 125
  br.clear
  .profile-left Name
  .profile-right
    = @user.full_name
    = " (Admin)" if @user.permitted?(:admin)
  br.clear
  .profile-left Email
  .profile-right = @user.email
  br.clear
  .profile-left Last login
  .profile-right = @user.last_sign_in_at.blank? ? "Never logged in" : @user.last_sign_in_at
  br.clear
  .profile-left From
  .profile-right = @user.last_sign_in_at.blank? ? "" : @user.last_sign_in_ip
  hr.clear
  - if current_user.id == @user.id
    .center = link_to "Update your information", edit_user_path
