h1 Thank you for registering!

.information If you would like, you can pay for this registration now. If you would prefer to pay later, feel free to close this tab.
hr

- if ENV['ASSET_ENVIRONMENT'] == 'production' && ENV['FB_TRACK_REGISTRATIONS'] == 'enabled'
  script fbq('track', 'CompleteRegistration');

= render 'braintree/dropin'
